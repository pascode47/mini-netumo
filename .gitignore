# Node / NPM / Yarn
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Usually committed, but can be ignored if only package.json is source of truth
# pnpm-lock.yaml # If using pnpm

# Backend (Node.js / TypeScript specific)
backend/dist/
backend/node_modules/
# backend/.env # Temporarily allow backend/.env for deployment convenience
backend/coverage/
backend/*.log
backend/build_log.txt # Assuming this is a temporary build log

# Frontend (Angular specific)
frontend/node_modules/
frontend/dist/
frontend/.angular/
frontend/coverage/
frontend/npm-debug.log*
frontend/yarn-debug.log*
frontend/yarn-error.log*
# frontend/package-lock.json # Usually committed for Angular projects

# Docker
docker-compose.override.yml
*.dockerignore # Dockerignore files themselves are usually versioned

# IDE / Editor specific
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
# These are often specific to projects but node_modules is the most common
# deps/

# Optional: Sass cache files
.sass-cache/
.node-sass/

# Optional: Typescript cache
*.tsbuildinfo

# Environment files (general, if not in backend/ or frontend/)
# .env # Temporarily allow root .env if used
# .env.local
# .env.development.local
.env.test.local
.env.production.local
.env.*.local

# Optional: Compiled files from other languages if interop
# *.pyc
# *.pyo
# *.rbc
# *.gem

# Backup files
*.bak
*~

# Mongo Backups (if stored locally and not intended for repo)
mongo_backups/

# Build log if it's generated at root
# build_log.txt # Already covered by *.log or specific path if needed

# If PROJECT_CONTRIBUTIONS.md was temporary and README.md is the final one
PROJECT_CONTRIBUTIONS.md
